# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
# ENVIRONMENT VARIABLES - EXAMPLE FILE (.env.example)
# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
# Copy this file to create your .env files in backend/ and frontend/ folders
# Usage:
#   - Backend: Copy to backend/.env
#   - Frontend: Copy to frontend/.env
# ════════════════════════════════════════════════════════════════════════════════════════════════════════════

# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
# BACKEND ENVIRONMENT VARIABLES (backend/.env)
# ════════════════════════════════════════════════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# SERVER CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
PORT=5000                                    # Server port
NODE_ENV=development                         # Environment: development, staging, production
HOST=localhost                               # Server host

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# DATABASE CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# For Local MongoDB:
MONGO_URI=mongodb://localhost:27017/products

# For MongoDB Atlas (Cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/products

# For development without database (uses mock data):
# MONGO_URI=                                  # Leave empty to use mock data

# Database name (will be appended to connection string if not present)
# DB_NAME=products

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# SHOPIFY INTEGRATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_PASSWORD=your_shopify_api_password
SHOPIFY_STORE_URL=your_store.myshopify.com
SHOPIFY_API_VERSION=2024-01

# How to get Shopify credentials:
# 1. Go to https://accounts.shopify.com/
# 2. Navigate to Apps and sales channels > App and sales channel settings
# 3. Develop apps and sales channels > Create a new app
# 4. Set permissions for products (read_products)
# 5. Install and get API key/password

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# AMAZON INTEGRATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
AMAZON_API_KEY=your_amazon_api_key
AMAZON_API_SECRET=your_amazon_api_secret
AMAZON_REGION=us-east-1                      # AWS region (us-east-1, eu-west-1, etc.)
AMAZON_SELLER_ID=your_seller_id

# How to get Amazon credentials:
# 1. Go to https://sellercentral.amazon.com/
# 2. Navigate to Apps & Integrations > SP-API Applications
# 3. Create app and generate LWA tokens
# 4. Get API key and secret from developer console

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# API SETTINGS
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
API_VERSION=v1                               # API version
API_PREFIX=/api                              # API route prefix
API_TIMEOUT=30000                            # Request timeout (milliseconds)

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# CORS & SECURITY
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:3000            # Primary CORS origin
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://127.0.0.1:3000,http://127.0.0.1:5000
ALLOW_CREDENTIALS=true                       # Allow credentials in CORS

# For production, update CORS_ORIGIN to your frontend domain:
# CORS_ORIGIN=https://yourdomain.com

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# LOGGING
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
LOG_LEVEL=info                               # Log level: error, warn, info, debug, trace
LOG_FORMAT=combined                          # Log format: combined, common, dev, short, tiny

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# MOCK DATA
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
USE_MOCK_DATA=true                           # Use mock data when database unavailable
MOCK_DATA_COUNT=100                          # Number of mock products to generate

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# PAGINATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
DEFAULT_PAGE_SIZE=20                         # Default items per page
MAX_PAGE_SIZE=100                            # Maximum items per page


# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
# FRONTEND ENVIRONMENT VARIABLES (frontend/.env)
# ════════════════════════════════════════════════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# API CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
REACT_APP_API_URL=http://localhost:5000/api  # Backend API URL
REACT_APP_API_TIMEOUT=30000                  # API request timeout (milliseconds)
REACT_APP_API_VERSION=v1                     # API version

# For production, update REACT_APP_API_URL:
# REACT_APP_API_URL=https://yourdomain.com/api

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# APP CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
REACT_APP_NAME=Product Manager               # Application name
REACT_APP_VERSION=1.0.0                      # Application version
REACT_APP_DESCRIPTION=Manage products from Shopify and Amazon

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# UI CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
REACT_APP_ITEMS_PER_PAGE=20                  # Products per page in grid
REACT_APP_THEME=light                        # Theme: light, dark
REACT_APP_LANGUAGE=en                        # Language: en, es, fr, etc.

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# ENVIRONMENT
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
REACT_APP_ENV=development                    # Environment: development, staging, production
REACT_APP_DEBUG=false                        # Enable debug mode (true/false)

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# FEATURE FLAGS
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
REACT_APP_ENABLE_ANALYTICS=false              # Enable analytics tracking
REACT_APP_ENABLE_NOTIFICATIONS=true           # Enable toast notifications
REACT_APP_ENABLE_EXPORT=true                  # Enable Excel export
REACT_APP_ENABLE_SYNC=true                    # Enable sync buttons

# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
# INTEGRATIONS
# ──────────────────────────────────────────────────────────────────────────────────────────────────────────────
REACT_APP_SHOPIFY_ENABLED=true                # Enable Shopify features
REACT_APP_AMAZON_ENABLED=true                 # Enable Amazon features


# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
# CONFIGURATION GUIDE
# ════════════════════════════════════════════════════════════════════════════════════════════════════════════

# 1. SETUP INSTRUCTIONS:
#    - Copy this file: cp .env.example backend/.env
#    - Copy this file: cp .env.example frontend/.env
#    - Edit files with your actual values

# 2. REQUIRED vs OPTIONAL:
#    REQUIRED:
#      - PORT (backend)
#      - NODE_ENV (backend)
#      - REACT_APP_API_URL (frontend)

#    OPTIONAL:
#      - All API keys (will use mock data if not provided)
#      - MONGO_URI (will use mock data if not provided)

# 3. DEVELOPMENT MODE (No external services):
#    Backend .env:
#      - Keep MONGO_URI empty or commented
#      - USE_MOCK_DATA=true
#    Frontend .env:
#      - REACT_APP_API_URL=http://localhost:5000/api

# 4. PRODUCTION MODE (Real services):
#    Backend .env:
#      - Set MONGO_URI to production database
#      - Set all API keys
#      - NODE_ENV=production
#    Frontend .env:
#      - REACT_APP_API_URL=https://yourdomain.com/api
#      - REACT_APP_ENV=production

# 5. SECURITY:
#    - Never commit .env files to version control
#    - Use .gitignore to exclude .env files
#    - Keep API keys secret
#    - Use strong database passwords
#    - Enable CORS only for trusted origins

# 6. TROUBLESHOOTING:
#    - Check .env file is in correct folder (backend/ or frontend/)
#    - Make sure variable names are spelled correctly
#    - For React, all variables must start with REACT_APP_
#    - Restart servers after changing .env files
#    - Check logs for environment variable errors

# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
# END OF ENVIRONMENT VARIABLES EXAMPLE
# ════════════════════════════════════════════════════════════════════════════════════════════════════════════
